import { StyleSheet } from "react-native";
import { rs } from "@/core/responsive";

export default StyleSheet.create({
	weekdaySelector: {
		flexDirection: "row",
		flexWrap: "wrap",
		gap: rs(8),
	},
});

